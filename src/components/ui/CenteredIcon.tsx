import 'bootstrap/dist/css/bootstrap.min.css';

const CenteredIcon = ({children}: any) => (
  <div className="d-flex justify-content-center align-items-center">
    {children}
  </div>
);

export default CenteredIcon;